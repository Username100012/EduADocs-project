# Copy this file to .env and fill in your API keys
# Then load it (e.g., `export $(cat .env | xargs)` on Linux/macOS or use PowerShell `Get-Content .env | foreach { $name,$value = $_ -split '='; setx $name $value }`)

# OpenAI API Key (required for OpenAI models)
OPENAI_API_KEY=your_openai_api_key_here

# Google API Key (required for Google GenAI models)
GOOGLE_API_KEY=your_google_api_key_here

# Hugging Face API Key (optional, for private models or higher rate limits)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# DeepSeek API Key (if you want to use DeepSeek models)
DEEPSEEK_API_KEY=your_deepseek_api_key_here
